import React, { useState } from 'react';import styled, { css } from "styled-components";import colors from "../../styles/colors";import {BsFillQuestionCircleFill} from "react-icons/bs";import {useSelector} from "react-redux";const ToolTipIcon = styled.span`  font-size: 16px;  color: ${colors.deepDarkGray};  position: relative;  &:hover {    cursor: help;  }    `;const ToolTipBox = styled.div`  width: 250px;  font-size: 12px;  color: ${colors.lightBlack};  border-radius: 5px;  border: 1px solid ${colors.chatDefaultColor};  word-break: keep-all;  background: ${colors.deepWhite};  padding: 10px;  position: absolute;  top: 20px;  left: 0;  z-index: 1;  visibility: hidden;  box-shadow: 0 0 8px ${colors.gray};    @media screen and (min-width: 430px) {    ${ToolTipIcon}:hover & {      visibility: visible;    }  }    @media screen and (max-width: 430px) {    left: -150px;    visibility: ${({mobileClick}) => mobileClick ? 'visible' : 'hidden'};  }`;const ToolTip = ({ children }) => {    const [mobileClick, setMobileClick] = useState(false);    const { isMobile } = useSelector(state => state.isMobile);    const handleClick = () => setMobileClick(!mobileClick);    return (        <ToolTipIcon onClick={isMobile ? handleClick : undefined}>            <BsFillQuestionCircleFill/>            <ToolTipBox mobileClick={mobileClick}>                {children}            </ToolTipBox>        </ToolTipIcon>    )}export default ToolTip;